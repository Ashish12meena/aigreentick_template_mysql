/**
 * One {@code *UseCaseImpl} class per {@code application.port.in}
 * interface. Each class does exactly one business operation and is
 * the only place that operation's orchestration logic lives.
 *
 * Naming: interface {@code XxxUseCase} in {@code port.in},
 * implementation always {@code XxxUseCaseImpl} here — never reuse the
 * exact same simple name across packages.
 */
package com.aigreentick.services.template.application.usecase;
